{
    "compilerOptions": {
      "target": "ES2022",                                  // Define a versão do JavaScript alvo
      //"module": "commonjs",                             // Define o sistema de módulos
      "strict": true,                                   // Ativa o modo estrito
      "forceConsistentCasingInFileNames": true,         // Força o uso consistente de maiúsculas e minúsculas nos nomes dos arquivos
      "outDir": "./dist/js/",                               // Define o diretório de saída para os arquivos compilados
      "rootDir": "./src",                               // Define o diretório raiz para os arquivos fonte
      "esModuleInterop": true,                          // Habilita a interoperabilidade com módulos ES6
      "skipLibCheck": true,                             // Ignora a checagem de tipo em arquivos de declaração
      "strictNullChecks": true,                         // Habilita a checagem estrita de valores nulos
      "strictFunctionTypes": true,                      // Habilita a checagem estrita de tipos de função
      "strictPropertyInitialization": true,             // Habilita a checagem estrita de inicialização de propriedades
      "noImplicitAny": true,                            // Desabilita a inferência implícita do tipo `any`
      "noImplicitThis": true,                           // Desabilita o uso implícito do `this`
      "alwaysStrict": true,                             // Garante que o código é sempre executado no modo estrito
      "noUnusedLocals": true,                           // Gera erros para variáveis locais não utilizadas
      "noUnusedParameters": true,                       // Gera erros para parâmetros de função não utilizados
      "noImplicitReturns": true,                        // Gera erros se nem todos os caminhos de uma função retornarem um valor
      "noFallthroughCasesInSwitch": true,                // Gera erros para casos de switch que não têm break ou return
      "noEmitOnError": true,
      "experimentalDecorators": true,

    },
    "include": [
      "./src/**/*"                                        // Inclui todos os arquivos no diretório `src`
    ],
    "exclude": [
      "node_modules",                                   // Exclui o diretório `node_modules`
      "dist"                                            // Exclui o diretório `dist`
    ]
  }
  